# ============================================
# Python Template - Development Environment
# ============================================

# Application Info
SERVICE_NAME=Python Template
SERVICE_VERSION=v0.1.0
SERVICE_DESCRIPTION=Python FastAPI Template Service

# Server Configuration
HOST=0.0.0.0
SERVER_PORT=8020
LOG_LEVEL=DEBUG

# Docker Compose
COMPOSE_PROJECT_NAME=python_template
ENVIRONMENT=development

# Docker Network Configuration
DOCKER_NETWORK_WEB=core_web
DOCKER_NETWORK_INTERNAL=core_internal
DOCKER_NETWORK_EXTERNAL=true

# Bind address for port mapping (127.0.0.1 for localhost-only, 0.0.0.0 for all interfaces)
BIND_ADDRESS=127.0.0.1

# Traefik Configuration (disabled for local development)
TRAEFIK_ENABLED=false
TRAEFIK_DOMAIN=localhost:8020
TRAEFIK_NETWORK=core_web
TRAEFIK_ROUTER_NAME=python-template-dev
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERT_RESOLVER=le
